import { createTheme } from '@mui/material'

export default createTheme({
  palette: {
    primary: {
      main: '#0C0E14',
    },
  },
  breakpoints: {
    values: {
      xs: 0,
      sm: 600,
      md: 900,
      lg: 1200,
      xl: 1536,
    },
  },
})
